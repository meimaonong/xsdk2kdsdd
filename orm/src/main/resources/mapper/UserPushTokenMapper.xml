<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art.app.orm.dao.UserPushTokenMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.art.app.orm.entity.UserPushToken">
        <id column="id" property="id"/>
        <result column="client_id" property="clientId"/>
        <result column="user_id" property="userId"/>
        <result column="source" property="source"/>
        <result column="token" property="token"/>
        <result column="del_flag" property="delFlag"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, client_id AS clientId, user_id AS userId, source, token, del_flag AS delFlag, created_at AS createdAt, updated_at AS updatedAt
    </sql>

    <insert id="replace" parameterType="com.art.app.orm.entity.UserPushToken">
        insert into user_push_token (client_id, user_id, source, token, created_at, del_flag)
        values (#{r.clientId}, #{r.userId}, #{r.source}, #{r.token}, #{r.createdAt}, #{r.delFlag})
        on duplicate key update token = #{r.token}, del_flag = #{r.delFlag}, updated_at = #{r.updatedAt}
    </insert>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art.app.orm.dao.MemberPointMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.art.app.orm.entity.MemberPoint">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="point" property="point"/>
        <result column="type" property="type"/>
        <result column="total" property="total"/>
        <result column="source" property="source"/>
        <result column="del_flag" property="delFlag"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <resultMap id="PointMap" type="com.art.app.orm.bean.MemberCurrentPoints">
        <result column="user_id" property="userId"/>
        <result column="points" property="points"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id AS userId, point, type, total, source, del_flag AS delFlag, created_at AS createdAt, updated_at AS updatedAt
    </sql>

    <select id="getUserPointsByType" resultMap="PointMap">
        select user_id, sum(point) as points from member_point where del_flag = 0 and type = #{type} group by user_id;
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art.app.orm.dao.CouponMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.art.app.orm.entity.Coupon">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="amount" property="amount"/>
        <result column="valid_start_at" property="validStartAt"/>
        <result column="valid_end_at" property="validEndAt"/>
        <result column="stock" property="stock"/>
        <result column="dispatched" property="dispatched"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="del_flag" property="delFlag"/>
        <result column="detail" property="detail"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, type, amount, valid_start_at AS validStartAt, valid_end_at AS validEndAt, stock, dispatched, created_at AS createdAt, updated_at AS updatedAt, del_flag AS delFlag, detail
    </sql>

</mapper>
